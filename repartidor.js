function cambiarEstado(nuevoEstado) {
    let estadoTexto = document.getElementById("estado");

    if (estadoTexto) {
        estadoTexto.textContent = "Estado actual: " + nuevoEstado;
    }
}
function cambiarEstado1(nuevoEstado) {
    let estadoTexto = document.getElementById("estado1");

    if (estadoTexto) {
        estadoTexto.textContent = "Estado actual: " + nuevoEstado;
    }
}